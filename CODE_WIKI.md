# ProTrader 交易复盘控制台 — Code Wiki

## 一、项目概述

### 1.1 项目简介

**ProTrader** 是一款专为交易者设计的复盘控制台，基于浏览器运行的单页应用（SPA）。项目以 Apache License 2.0 开源，使用纯前端技术栈构建，无需后端即可独立运行。核心功能涵盖交易数据仪表盘、历史交易日志、计划看板以及深度统计分析四大模块，帮助交易者系统化地追踪交易记录、分析交易绩效并优化交易策略。

### 1.2 技术选型

| 技术领域 | 选型方案 | 用途说明 |
|----------|----------|----------|
| 前端框架 | Vue 3 (Composition API) | 响应式状态管理与组件化开发 |
| 构建工具 | 无（CDN 引入） | 通过 CDN 直接加载，简化部署 |
| 样式框架 | Tailwind CSS 3.x | 原子化 CSS，快速构建深色主题 UI |
| 图表库 | ECharts 5.4.3 | 资金曲线、品种分布等数据可视化 |
| 图标库 | Font Awesome 6.0 | 统一的图标系统 |
| 字体 | Inter（正文）+ Roboto Mono（数字） | 专业阅读体验与等宽数字显示 |

### 1.3 文件结构

```
/workspace/
├── index.html          # 完整应用入口（HTML + CSS + Vue 代码全部内联）
└── LICENSE             # Apache License 2.0
```

项目采用单文件部署模式，所有代码集中于 `index.html`，包括 HTML 结构、CSS 样式、Vue 应用逻辑以及 ECharts 图表配置。这种方式极大降低了部署复杂度，用户只需在浏览器中打开该文件即可运行。

---

## 二、整体架构

### 2.1 应用架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        HTML 文档结构                              │
├──────────────┬──────────────────────────────────────────────────┤
│              │                    Header                         │
│   Sidebar    │  (当前视图名称 + 账户净值 + 本月盈亏 + 用户图标)      │
│   (侧边栏)    ├──────────────────────────────────────────────────┤
│              │                                                   │
│  • Dashboard │              Main Content Area                    │
│  • Journal   │              (路由驱动的视图区域)                    │
│  • Kanban    │                                                   │
│  • Analytics │   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐│
│              │   │Dashboard│ │ Journal │ │ Kanban  │ │Analytics││
│              │   │  View   │ │  View   │ │  View   │ │  View  │ │
│  [记录交易]   │   └─────────┘ └─────────┘ └─────────┘ └────────┘│
│              │                                                   │
└──────────────┴──────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Vue 3 应用层 (Composition API)               │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐     │
│  │   状态管理      │  │   计算属性      │  │    方法        │     │
│  │  (ref/reactive)│  │  (computed)    │  │  (functions)  │     │
│  └────────────────┘  └────────────────┘  └────────────────┘     │
│                              │                                   │
│                              ▼                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              ECharts 图表层                                  │ │
│  │   资金增长曲线 (Line Chart)  │  品种分布 (Pie Chart)         │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 视图模块划分

应用通过 `currentView` 状态变量驱动视图切换，支持四个主要视图：仪表盘（Dashboard）、交易日志（Journal）、计划看板（Kanban）和深度统计（Analytics）。每个视图对应一个条件渲染的 `<div>` 容器，通过 `v-show` 指令控制显示与隐藏，避免频繁的 DOM 销毁与重建。

侧边栏作为全局导航，固定在页面左侧，包含四个导航链接和一个醒目的“记录交易”按钮。主内容区域占据剩余空间，顶部 header 显示当前账户状态的关键指标。这种布局设计参考了专业交易终端的惯用模式，确保关键信息始终可见。

---

## 三、主要模块职责

### 3.1 状态管理层

状态管理完全依托 Vue 3 的 Composition API，不依赖 Vuex 或 Pinia。所有响应式状态通过 `ref()` 和 `computed()` 定义，集中于 `setup()` 函数内部。

**核心状态变量**：

| 变量名 | 类型 | 说明 |
|--------|------|------|
| `currentView` | `ref<string>` | 当前激活的视图标识，取值为 `dashboard`、`journal`、`kanban`、`analytics` |
| `showAddModal` | `ref<boolean>` | 控制新增交易弹窗的显示与隐藏 |
| `accountBalance` | `ref<number>` | 当前账户净值，初始值设为 $100,000 |
| `trades` | `ref<Trade[]>` | 交易记录数组，存储所有交易数据 |
| `filters` | `ref<{symbol, status}>` | 交易日志的筛选条件 |
| `newTrade` | `ref<NewTradeForm>` | 新交易表单的临时数据对象 |
| `kanbanColumns` | `Array<{id, title}>` | 看板列的配置定义 |

**交易数据结构（Trade）**：

```typescript
interface Trade {
    id: number;           // 唯一标识，时间戳生成
    date: string;         // 交易日期，ISO 格式 YYYY-MM-DD
    symbol: string;       // 交易品种，如 BTCUSDT、ETHUSDT
    direction: 'Long' | 'Short';  // 交易方向
    entry: number;        // 入场价格
    sl: number;           // 止损价格
    tp: number;           // 止盈价格
    exit: number | null;  // 出场价格，未平仓则为 null
    rr: number | string;  // 盈亏比（Risk:Reward）
    pl: number;           // 盈亏金额
    status: 'WATCH' | 'OPEN' | 'WIN' | 'LOSS';  // 交易状态
    strategy: string;     // 使用的策略名称
    notes: string;       // 交易笔记与备注
}
```

### 3.2 计算属性层

计算属性（Computed Properties）根据响应式状态自动推导派生数据，无需手动调用更新。项目中定义的核心计算属性如下：

**`winRate`**：胜率计算属性。筛选出已平仓的交易（状态为 WIN 或 LOSS），统计获胜次数并除以总平仓次数，结果乘以 100 后取整。若无已平仓交易，返回 0。该属性用于仪表盘的关键指标展示。

**`avgRR`**：平均盈亏比计算属性。遍历所有交易，将盈亏比字段累加后除以交易总数，保留两位小数。由于已平仓交易和未平仓交易均参与计算，这反映了交易者整体的风险收益偏好。

**`monthlyPL`**：本月累计盈亏计算属性。将所有交易的盈亏金额（`pl` 字段）累加求和。当前实现对所有历史交易求和，未按月份过滤，实际项目中应结合交易日期进行月份筛选。

**`filteredTrades`**：过滤后的交易列表计算属性。根据用户输入的交易品种筛选条件（大小写不敏感）和交易状态筛选条件，返回符合条件的交易子集。筛选逻辑采用 AND 关系，即同时满足品种匹配和状态匹配的交易才会显示。

**`calculatedRR`**：表单盈亏比实时计算属性。当用户在新增交易弹窗中填写入场价、止损价和止盈价后，该属性动态计算预期的风险收益比。计算公式为：止盈与入场价之差的绝对值除以入场价与止损价之差的绝对值。若任一字段为空或风险为零，返回 0。

**`viewName`**：视图名称计算属性。根据 `currentView` 的值映射到中文视图名称，用于 Header 区域显示当前所在页面。

### 3.3 看板模块

看板模块实现了交易状态的可视化管理，采用经典的四列看板布局。列定义存储于 `kanbanColumns` 数组，包含观察列表（Watchlist）、持仓中（Open）、盈利结案（Winners）和亏损结案（Losers）。

`getKanbanTrades(status)` 方法根据列标识筛选对应的交易记录，返回该列下的所有卡片。`getKanbanCount(status)` 方法封装了对卡片数量的统计，用于在列标题旁显示计数徽章。

`moveKanban(trade, action)` 方法模拟交易状态的手动推进。当用户点击卡片上的"Next Step"按钮时，若交易当前处于观察列表状态则推进至持仓中，若处于持仓中则推进至盈利结案。这一设计允许交易者手动管理交易进展，但当前实现为简化版本，完整的流转逻辑应包含更多的状态校验与边界处理。

### 3.4 图表模块

图表模块基于 ECharts 实现，包含资金增长曲线（折线图）和品种表现分布（饼图）两个可视化组件。

**资金曲线初始化**：`initCharts()` 方法中的资金曲线部分，首先获取页面元素 `equityChart` 的 DOM 引用，初始化 ECharts 实例。然后对交易记录进行反转处理，从最早到最近排序，依次累加盈亏金额生成资金曲线数据点。最后配置折线图的样式，包括渐变填充效果、坐标轴颜色主题等，使其与整体深色主题保持一致。

**品种分布饼图**：饼图初始化逻辑首先统计获胜交易数量、亏损交易数量和持仓中交易数量，然后生成三类数据用于饼图展示。饼图采用环形布局（内半径 40%，外半径 70%），通过不同的颜色区分交易结果，绿色代表盈利、红色代表亏损、黄色代表持仓中。

**响应式处理**：在 `onMounted` 钩子中注册了 `resize` 事件监听器，当浏览器窗口大小变化时自动调用 ECharts 的 `resize()` 方法，确保图表始终填充容器并保持正确比例。此外，通过 `watch` 监听 `currentView` 变化，在切换回仪表盘视图时延迟 100 毫秒后重新初始化图表，以解决 DOM 渲染时序问题。

---

## 四、关键函数说明

### 4.1 核心业务函数

**`saveTrade()`**

新增交易记录的核心函数。当用户填写完交易表单并点击保存时触发。首先根据 `newTrade` 表单数据构建完整的交易对象，其中 `id` 使用 `Date.now()` 生成时间戳作为唯一标识，`date` 提取当前日期的 ISO 格式部分。然后将计算得出的盈亏比（`calculatedRR`）和默认盈亏金额（0）一并写入对象。接着使用 `unshift()` 方法将新交易插入数组头部，使其出现在列表最前方。最后关闭弹窗、重置表单数据，并调用 `initCharts()` 刷新图表以反映新增数据。

**`formatNumber(num)`**

数字格式化工具函数。接收任意数值参数，使用 `Intl.NumberFormat` 原生 API 格式化为千分位分隔符显示。例如 100000 格式化为 "100,000"。该函数用于账户净值、本月盈亏等金额字段的展示，提供更好的可读性。

**`initCharts()`**

图表初始化与更新的核心函数。由于 ECharts 需要在 DOM 元素挂载后才能初始化，该函数在组件挂载时和视图切换回仪表盘时调用。函数内部首先检查目标 DOM 元素是否存在，避免在元素不可见时（`display: none`）初始化导致的问题。资金曲线数据通过反向遍历交易数组并累加盈亏计算得出，饼图数据通过统计各状态交易数量生成。

### 4.2 辅助函数

**`getKanbanTrades(status)`**：接收看板列的状态标识，返回该状态下所有交易的过滤数组。

**`getKanbanCount(status)`**：接收看板列的状态标识，返回对应列的交易卡片数量，用于渲染列标题旁的数量徽章。

**`moveKanban(trade, action)`**：接收交易对象和操作标识，执行简化版的状态流转逻辑。当前实现仅处理 WATCH→OPEN→WIN 的单向流转。

---

## 五、依赖关系分析

### 5.1 外部依赖

项目通过 CDN 引入所有外部依赖，无需本地安装任何包。以下是完整的依赖清单及其加载顺序：

**Tailwind CSS**：通过 `cdn.tailwindcss.com` 加载，负责基础样式和布局。Tailwind 的 `darkMode: 'class'` 配置启用基于 CSS 类名的暗色模式切换。项目在 `<html>` 标签上预设了 `class="dark"`，因此所有样式默认应用暗色主题。Tailwind 的原子化类名（如 `bg-gray-900`、`text-blue-400`、`flex items-center`）遍布整个 HTML 模板，实现了设计与开发的解耦。

**Vue 3**：通过 `unpkg.com/vue@3` 加载完整版 Vue，包含运行时和编译器。项目中使用了 Vue 3 的四大核心 API：`ref` 用于创建响应式状态，`computed` 用于定义计算属性，`onMounted` 用于生命周期钩子，`watch` 用于监听响应式数据变化。模板中使用了 Vue 指令（`v-model`、`v-show`、`v-for`、`@click`）进行数据绑定和事件处理。

**ECharts**：通过 `cdnjs.cloudflare.com` 加载 ECharts 5.4.3 版本。项目仅使用了 ECharts 的核心图表功能，未引入扩展插件。图表初始化采用传统的命令式调用方式（`echarts.init()` + `setOption()`），而非 Vue-ECharts 的声明式封装。`echarts.graphic.LinearGradient` 用于实现图表的渐变填充效果。

**Font Awesome**：通过 CDN 加载 Font Awesome 6.0 的 CSS 文件，提供全系图标支持。项目广泛使用图标类名（如 `fa-solid fa-chart-line`、`fa-plus`、`fa-check-circle`）增强视觉层次。

### 5.2 CDN 资源加载时序

```
1. Tailwind CSS (脚本执行配置)
2. Vue 3 (CDN 加载)
3. ECharts (CDN 加载)
4. Font Awesome CSS (CDN 加载)
5. 自定义样式 (内联 <style>)
6. HTML 模板 (DOM 结构)
7. Vue 应用脚本 (内联 <script>)
```

由于 Vue 应用脚本位于 HTML 模板之后执行，且使用了 `onMounted` 钩子进行图表初始化，整个加载时序是安全可靠的。

---

## 六、项目运行方式

### 6.1 运行要求

本项目为零依赖单文件应用，运行要求极低：

- **浏览器**：支持现代浏览器的任意版本（Chrome、Firefox、Safari、Edge 等）
- **网络**：需要网络连接以加载 CDN 资源（Tailwind CSS、Vue 3、ECharts、Font Awesome）
- **无需**：Node.js、npm、构建工具或后端服务器

### 6.2 启动方式

**方式一（推荐）：直接打开**

双击 `/workspace/index.html` 文件，或右键选择"用浏览器打开"。文件将在默认浏览器中加载运行。

**方式二（本地服务器）**

若因浏览器安全策略限制（跨域问题、CSP 限制等）无法直接打开，可启动本地 HTTP 服务器：

```bash
# Python 3
cd /workspace
python -m http.server 8080

# 或使用 npx (需要 Node.js)
npx serve /workspace -p 8080
```

启动后访问 `http://localhost:8080` 即可。

### 6.3 功能入口

应用启动后，用户可通过侧边栏导航访问四大功能模块：

| 模块 | 入口描述 | 主要功能 |
|------|----------|----------|
| **仪表盘** | 侧边栏第一项，默认视图 | 展示总交易次数、胜率、平均盈亏比、最大回撤、资金增长曲线、品种表现分布 |
| **交易日志** | 侧边栏第二项 | 表格形式展示所有交易记录，支持品种搜索和状态筛选 |
| **计划看板** | 侧边栏第三项 | 四列看板布局管理交易状态，支持卡片拖拽流转 |
| **深度统计** | 侧边栏第四项 | 当前为占位页面，预告持仓时间分析、最大连续亏损分析等功能开发中 |

点击侧边栏底部的"记录交易"按钮可打开新增交易弹窗，填写交易信息后保存。

---

## 七、数据流与状态更新机制

### 7.1 数据流图

```
用户交互
    │
    ▼
Vue 事件处理 (@click, v-model)
    │
    ▼
更新响应式状态 (ref / reactive)
    │
    ├──▶ 计算属性重新计算 (computed)
    │        │
    │        ▼
    │    模板自动重新渲染
    │
    └──▶ 方法执行副作用
             │
             ▼
         DOM 更新 (initCharts → ECharts)
```

Vue 3 的响应式系统基于 Proxy 实现。当用户修改表单输入（`v-model` 双向绑定）或触发点击事件（`@click`）时，对应的响应式状态自动更新。计算属性作为依赖项的派生值，会在依赖变化时自动重新计算。模板中的表达式会在下一次微任务队列执行时批量更新，确保高效的 DOM 操作。

### 7.2 状态更新触发链

**场景一：添加新交易**

用户点击"记录交易"按钮 → `showAddModal` 变为 `true` → 弹窗显示 → 用户填写表单 → 点击保存 → `saveTrade()` 执行 → `trades` 数组更新（unshift 新元素）→ `filteredTrades` 重新计算 → 表格自动渲染新行 → `initCharts()` 被调用 → 图表数据更新并重绘。

**场景二：切换视图**

用户点击侧边栏导航项 → `currentView` 更新为目标视图 → `v-show` 控制显示对应的视图容器 → 若目标为仪表盘，延迟 100ms 后 `initCharts()` 被调用 → 图表在正确的 DOM 可见状态下初始化。

**场景三：筛选交易**

用户在交易日志视图输入搜索关键词 → `filters.symbol` 响应式更新 → `filteredTrades` 重新计算 → 表格自动过滤显示符合条件的记录。

---

## 八、样式系统

### 8.1 主题配置

项目采用深色主题设计，Tailwind 配置扩展了以下自定义颜色：

```javascript
tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                gray: { 900: '#0f172a', 800: '#1e293b', 700: '#334155' },
                tradeWin: '#10b981',  // 绿色：盈利
                tradeLoss: '#ef4444'  // 红色：亏损
            }
        }
    }
}
```

`tradeWin` 和 `tradeLoss` 两个自定义颜色贯穿整个应用，用于区分盈利交易（绿色）和亏损交易（红色），提供直观的视觉反馈。

### 8.2 数字字体

项目为数字内容（价格、盈亏金额、盈亏比等）专门应用了等宽字体 `Roboto Mono`，通过自定义工具类 `.number-font` 实现。这一设计确保了数字列在表格中的对齐效果，同时增强了数据的专业感。

### 8.3 滚动条样式

自定义滚动条样式针对深色主题优化：

```css
::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: #0f172a; }
::-webkit-scrollbar-thumb { background: #334155; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #475569; }
```

滚动条轨道颜色与页面背景一致（`#0f172a`），滑块颜色采用灰色系（`#334155`），悬停时变亮以提供交互反馈。

---

## 九、已知限制与改进方向

### 9.1 当前限制

**数据持久化缺失**：所有交易数据存储于内存（`trades` 响应式数组），刷新页面后数据将丢失。改进方案包括引入 `localStorage` 本地持久化或对接后端 API。

**月度盈亏计算不精确**：`monthlyPL` 计算属性当前对所有交易求和，未按月份过滤。需要结合交易日期字段实现真正的月度统计。

**看板流转逻辑简化**：`moveKanban()` 方法仅处理两种状态转移，缺少亏损结案（ LOSS ）状态的正确流转，以及从持仓中到亏损的转移逻辑。

**统计模块占位**：`analytics` 视图目前仅为占位页面，预告的功能（持仓时间分析、最大连续亏损分析、日历热力图等）尚未实现。

**图表时区与日期处理**：资金曲线图的日期轴未处理时区和本地化显示，当交易数量增加时，日期标签可能出现拥挤。

### 9.2 潜在改进

- 引入数据持久化层（`localStorage` 或 IndexedDB）
- 完善看板状态流转，支持双向转移和删除操作
- 实现高级统计分析功能
- 支持数据导入导出（CSV/JSON 格式）
- 增加交易编辑与删除功能
- 引入策略标签管理与统计
- 实现多账户管理
- 添加风险指标计算（如夏普比率、索提诺比率）

---

## 十、总结

ProTrader 项目展示了一个完整的单页应用架构设计，以简洁的技术栈实现了交易复盘的核心需求。Vue 3 Composition API 的运用使状态管理与业务逻辑清晰分离，Tailwind CSS 的原子化样式确保了 UI 的一致性与可维护性，ECharts 则提供了专业级的数据可视化能力。整体代码结构扁平（单文件约 470 行），适合快速理解与二次开发。项目采用 Apache License 2.0 开源，为交易者社区提供了一个可自由使用和定制的复盘工具基础。
